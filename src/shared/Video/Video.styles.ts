import styled from 'styled-components';

export const VideoStyled = styled.iframe`
  position: relative;
  border: none;
  display: block;
  width: 100%;
  height: 500px;
  padding: 10px 0;
  z-index: 1;
`;
